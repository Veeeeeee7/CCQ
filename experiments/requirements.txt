# Python 3.11. Install vllm first (it pins torch), then this file:
#
#   conda create -n ccq python=3.11 -y
#   pip install vllm --extra-index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements.txt
#   conda install -y -c conda-forge "ffmpeg>=6,<8"
#
# Match the torch CUDA build to the driver on the machine that runs the jobs.

# Data handling and metrics.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.5

# Gradient boosting. Capped below 3.x: shap cannot parse its serialized trees.
xgboost>=2.0,<3

# TreeSHAP attribution.
shap>=0.44

# Tabular deep learning.
torch>=2.0
pytorch-tabnet>=4.1
tabpfn>=2.0

# ModernBERT and Qwen3.
transformers>=4.51
accelerate>=0.30

# LoRA adapters.
peft>=0.11

# CORN ordinal head and loss.
coral-pytorch>=1.4

# Sentence embeddings. Capped below 5.x: it hard-imports torchcodec, which
# dlopens a CUDA runtime library and fails against a mismatched torch build.
sentence-transformers>=3.0,<5

# LLM inference backend.
vllm>=0.8

# Figures.
matplotlib>=3.7
